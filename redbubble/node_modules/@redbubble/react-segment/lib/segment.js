"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var get = require('lodash/get');
/* eslint-disable */


var analytics = function () {
  var analytics = window.analytics = window.analytics || [];

  if (!analytics.initialize) {
    if (analytics.invoked) window.console && console.error && console.error('Segment snippet included twice.');else {
      analytics.invoked = !0;
      analytics.methods = ['trackSubmit', 'trackClick', 'trackLink', 'trackForm', 'preview', 'identify', 'reset', 'group', 'track', 'ready', 'alias', 'debug', 'page', 'once', 'off', 'on', 'addSourceMiddleware', 'addIntegrationMiddleware', 'setAnonymousId', 'addDestinationMiddleware'];

      analytics.factory = function (e) {
        return function () {
          var t = Array.prototype.slice.call(arguments);
          t.unshift(e);
          analytics.push(t);
          return analytics;
        };
      };

      for (var e = 0; e < analytics.methods.length; e++) {
        var key = analytics.methods[e];
        analytics[key] = analytics.factory(key);
      }

      analytics.load = function (key, e) {
        var cdnSourceDomain = get(e, 'cdnSourceDomain', 'cdn.segment.com');
        var t = document.createElement('script');
        t.type = 'text/javascript';
        t.async = !0;
        t.src = "https://".concat(cdnSourceDomain, "/analytics.js/v1/").concat(key, "/analytics.min.js");
        var n = document.getElementsByTagName('script')[0];
        n.parentNode.insertBefore(t, n);
        analytics._loadOptions = e;
      }; // 4.13.2 is required for analytics.js v2


      analytics.SNIPPET_VERSION = '4.13.2';
    }
  }

  return analytics;
}();

var _default = analytics;
exports["default"] = _default;